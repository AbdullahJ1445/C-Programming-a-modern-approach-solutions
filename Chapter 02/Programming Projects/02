#include <stdio.h>
#include <math.h>

#define M_PI 3.14159265358979323846

int main()
{
    int r = 10;
    float v = (4.0f/3.0f) * (M_PI) * (r * r * r);
    
    printf("The volume of a sphere is: %.2f cubic meters.\n", v);
    
    return 0;
}
