#include <stdio.h>
#include <stdlib.h>

int main()
{
  int n, first, second;

  printf("Enter a two-digit number: ");
  scanf("%d", &n);

  first = n % 10;
  second = n / 10;

  printf("The reversal is: %d%d", first, second);

  return 0;
}
